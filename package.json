{
  "name": "bubassh",
  "version": "1.0.0",
  "description": "BubaSSH — a minimalist dark FTP/SFTP client for Linux, Windows and macOS.",
  "author": "mrcl77",
  "license": "MIT",
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "start": "electron-vite preview",
    "build": "electron-vite build",
    "icons": "node scripts/gen-icons.mjs",
    "dist": "npm run icons && npm run build && electron-builder --publish never",
    "dist:mac": "npm run icons && npm run build && electron-builder --mac --publish never",
    "dist:win": "npm run icons && npm run build && electron-builder --win --publish never",
    "dist:linux": "npm run icons && npm run build && electron-builder --linux --publish never",
    "test:ftp": "node scripts/test-ftp.mjs",
    "test:sftp": "node scripts/test-sftp.mjs",
    "test:profiles": "node scripts/test-profiles.mjs"
  },
  "dependencies": {
    "basic-ftp": "^5.0.5",
    "ssh2-sftp-client": "^11.0.0"
  },
  "devDependencies": {
    "electron": "^31.7.6",
    "electron-builder": "^24.13.3",
    "electron-vite": "^2.3.0",
    "ftp-srv": "^4.6.3",
    "png2icons": "^2.0.1",
    "sharp": "^0.33.5",
    "vite": "^5.4.11"
  }
}
